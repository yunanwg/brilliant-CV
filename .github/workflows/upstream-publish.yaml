name: Create PR for Upstream Publishing

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 2.0.7 or 2.0.7-test)'
        required: true
        default: '2.0.7'
        type: string
        
permissions:
  contents: read
  pull-requests: write
  actions: read

jobs:
  create-upstream-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout current repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract and validate version
        id: version
        run: |
          VERSION="${{ github.event.inputs.version }}"
          
          # Fallback to default if input is empty
          if [ -z "$VERSION" ] || [ "$VERSION" = "null" ]; then
            echo "⚠️ version input is empty, using default"
            VERSION="2.0.7"
          fi
          
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"
          
          # Validate version format (allow test suffix)
          if ! echo "$VERSION" | grep -E '^[0-9]+\.[0-9]+\.[0-9]+(-test)?$'; then
            echo "❌ Invalid version format: $VERSION"
            echo "Expected format: X.Y.Z or X.Y.Z-test (e.g., 2.0.7 or 2.0.7-test)"
            exit 1
          fi
          echo "✅ Version format valid: $VERSION"

      - name: Update typst.toml version
        run: |
          # Create backup and update version
          cp typst.toml typst.toml.backup
          sed -i 's/^version = ".*"/version = "${{ steps.version.outputs.version }}"/' typst.toml
          
          echo "=== Updated typst.toml ==="
          echo "Original version:"
          grep '^version = ' typst.toml.backup
          echo "New version:"
          grep '^version = ' typst.toml

      - name: Prepare package files
        run: |
          # Create package directory structure
          PACKAGE_DIR="packages/preview/brilliant-cv/${{ steps.version.outputs.version }}"
          mkdir -p "$PACKAGE_DIR"
          
          echo "=== Preparing package files ==="
          echo "Target directory: $PACKAGE_DIR"
          
          # Copy all files except excluded ones (from typst.toml exclude list)
          rsync -av \
            --exclude='.git' \
            --exclude='docs' \
            --exclude='.pre-commit-config.yaml' \
            --exclude='.gitignore' \
            --exclude='.github' \
            --exclude='packages' \
            --exclude='typst.toml.backup' \
            . "$PACKAGE_DIR/"
          
          echo "=== Package structure ==="
          find "$PACKAGE_DIR" -type f | sort
          
          echo "=== Package typst.toml content ==="
          cat "$PACKAGE_DIR/typst.toml"
          
          echo "=== Package size ==="
          du -sh "$PACKAGE_DIR"
          
          # Validate required files exist
          if [ ! -f "$PACKAGE_DIR/typst.toml" ]; then
            echo "❌ Missing typst.toml in package"
            exit 1
          fi
          
          if [ ! -f "$PACKAGE_DIR/lib.typ" ]; then
            echo "❌ Missing lib.typ (entrypoint) in package"
            exit 1
          fi
          
          echo "✅ Package validation passed"

      - name: Create PR in forked repository
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "=== Creating PR in forked repository ==="
          
          # Set up git config
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Clone the forked repository using GitHub CLI
          gh repo clone yunanwg/our-Typst-packages temp-fork
          cd temp-fork
          
          # Sync with upstream
          git remote add upstream https://github.com/typst/packages.git
          git fetch upstream main
          git checkout main
          git reset --hard upstream/main
          
          # Create new branch
          BRANCH_NAME="brilliant-cv-${{ steps.version.outputs.version }}"
          git checkout -b "$BRANCH_NAME"
          
          # Copy package files
          mkdir -p "packages/preview/brilliant-cv"
          cp -r "../packages/preview/brilliant-cv/${{ steps.version.outputs.version }}" "packages/preview/brilliant-cv/"
          
          # Commit changes
          git add .
          git commit -m "feat: add brilliant-cv v${{ steps.version.outputs.version }}

          Package Details:
          - Name: brilliant-cv
          - Version: ${{ steps.version.outputs.version }}
          - Repository: ${{ github.server_url }}/${{ github.repository }}
          - Workflow run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          
          Description:
          💼 another CV template for your job application, yet powered by Typst and more
          
          Features:
          - Separation of style and content
          - Quick visual customizations  
          - Multilingual support (EN, FR, DE, CN, IT)
          - AI Prompt and Keywords Injection"
          
          # Push branch and create PR using GitHub CLI
          git push -u origin "$BRANCH_NAME"
          
          gh pr create \
            --repo yunanwg/our-Typst-packages \
            --title "Add brilliant-cv v${{ steps.version.outputs.version }}" \
            --body "## Package Submission: brilliant-cv v${{ steps.version.outputs.version }}

          **Package Details:**
          - **Name**: brilliant-cv  
          - **Version**: ${{ steps.version.outputs.version }}
          - **Source Repository**: ${{ github.server_url }}/${{ github.repository }}
          - **Workflow Run**: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

          ## Package Information
          **Description**: 💼 another CV template for your job application, yet powered by Typst and more

          **Key Features**:
          - ✨ Separation of style and content
          - 🎨 Quick visual customizations
          - 🌍 Multilingual support (English, French, German, Chinese, Italian)
          - 🤖 AI Prompt and Keywords Injection
          - 📄 Both CV and Cover Letter templates

          **Categories**: cv, languages, layout  
          **License**: Apache-2.0

          ## Package Structure
          \`\`\`
          packages/preview/brilliant-cv/${{ steps.version.outputs.version }}/
          ├── lib.typ (entrypoint)
          ├── typst.toml (manifest)
          ├── cv.typ
          ├── letter.typ
          ├── template/ (template files)
          ├── utils/ (utility modules)
          └── ... (other package files)
          \`\`\`

          ## Validation Checklist
          - [x] Package structure follows Typst packages format
          - [x] typst.toml manifest is valid with correct version
          - [x] Version format is correct
          - [x] Entrypoint file exists
          - [x] Template files are included
          - [x] Excluded files are properly filtered

          ---
          *This PR was automatically created by the upstream publishing workflow.*" \
            --head "$BRANCH_NAME" \
            --base main

          echo "✅ PR created successfully"

      - name: Upload package as artifact
        uses: actions/upload-artifact@v4
        with:
          name: brilliant-cv-${{ steps.version.outputs.version }}
          path: packages/preview/brilliant-cv/${{ steps.version.outputs.version }}/
          retention-days: 7

      - name: Summary
        run: |
          echo "=== WORKFLOW COMPLETED ==="
          echo "✅ Version: ${{ steps.version.outputs.version }}"
          echo "✅ Target repository: yunanwg/our-Typst-packages"
          echo "✅ PR created with package files"
          echo "✅ Package uploaded as workflow artifact"
          echo ""
          echo "🔗 **Next Steps:**"
          echo "1. Visit https://github.com/yunanwg/our-Typst-packages to review the PR"
          echo "2. Test the package structure and files"
          echo "3. If everything looks good, this workflow is ready for production use"
          echo "4. For production: change target repo to 'typst/packages'"
          
          # Restore original typst.toml
          if [ -f "typst.toml.backup" ]; then
            mv typst.toml.backup typst.toml
            echo "✅ Original typst.toml restored"
          fi